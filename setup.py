from setuptools import setup

setup(
    name='Petroineos',
    version='0.1',
    packages=['Petroineos'],
    install_requires=[
        'pandas',
    ],
    python_requires='>=3.7',  # Specify your Python version requirement here
)